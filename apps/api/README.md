# API (Node.js + Express)

ملف البيئة: انسخ .env.example إلى .env وعدّل القيم وفقاً لبيئتك.

تشغيل التطوير:
- npm install
- npm run dev

Routes:
- GET /api/health
- (ستضاف لاحقاً) /api/auth, /api/customers, /api/devices, /api/tickets
